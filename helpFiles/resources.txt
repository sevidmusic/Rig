  ~~~
  # resources 
  ~~~~~
  ~~~
  An App's `resources` directory is for any miscellaneous files
  or directories used by the App.
  ~~~~~
  ~~~
  For example, images used by an App could be placed in the 
  App's `resources` directory, or a sub-directory of the App's
  `resources` directory.
  ~~~~~
